# File Workshop V1.0 开发计划

> 文档编号：FW-PLAN-V1.0  
> 文档版本：V3.9
> 文档状态：执行中  
> 编制日期：2026-08-05  
> 最近修订：2026-08-14
> 默认路径：`docs/File-Workshop-V1.0-开发计划.md`  
> 本次修订：完成 Windows 本地阶段收敛说明

## 1. 如何阅读本计划

本计划首先回答三个问题：

1. 系统由哪些模块组成；
2. 当前正在开发哪个模块；
3. 每个模块还剩哪些工程任务和测试。

项目按以下顺序实施：

1. 完成基础开发环境和后端工程骨架；
2. 以开发设计文档第 6 章的 16 个后端模块为开发主线，默认按章节顺序推进，也可根据依赖、环境和优先级灵活调整；
3. 每个后端模块完成时，立即追加到统一 API 接口文档；后端全部完成后只做汇总校验和冻结；
4. 按同一模块顺序，以累计 API 接口文档和对应 OpenAPI 为依据执行接口测试；
5. 接口稳定后，按同一模块顺序开发前端；
6. 完成前后端联调、端到端测试、非功能验证和发布验收。

日志、错误处理、分页、Request ID、代码生成和测试脚本属于工程能力，不作为独立业务模块穿插在主计划中；它们在基础准备中建立，并在每个系统模块内持续使用和验证。

## 2. 权威依据和模块边界

- `AGENTS.md`：项目常驻开发规则；
- `docs/File-Workshop-V1.0-企业级文件管理系统开发设计文档.md`：业务语义、安全边界，以及第 6 章系统模块划分；
- `docs/File-Workshop-V1.0-数据库设计说明.md`：数据库表、字段、类型、约束和索引的唯一权威来源；
- `docs/File-Workshop-V1.0-技术选型说明.md`：技术栈和工程工具；
- `backend/api/openapi.yaml`：开发期间的 REST API 工作契约；后端完成后冻结为接口测试唯一依据。

本计划只规定开发顺序、工程任务、状态和完成标准，不在这里重新发明业务规则、数据库字段或接口 Schema。

模块边界必须遵守设计文档第 6.18 节：接入层只调用应用服务，跨模块写入通过模块接口，查询不得绕过权限，模块之间不得形成循环依赖。

## 3. 状态和维护方式

### 3.1 状态标记

| 标记 | 状态 | 含义 |
|---|---|---|
| `[ ]` | 未开始 | 尚未开始开发 |
| `[~]` | 开发中 | 已开始，但尚未满足完成标准 |
| `[x]` | 已完成 | 实现、测试、文档和完成记录均已齐全 |
| `[!]` | 被阻塞 | 缺少必要环境、权限、用户决策或外部条件 |
| `[-]` | 暂缓 | 项目负责人明确决定延后 |

### 3.2 计划更新规则

1. 开始开发前，先在本计划中找到对应系统模块和工程任务。
2. 开始模块时，把模块及当前工程任务改为 `[~]`。
3. 完成一项工程任务后立即更新状态；完成整个模块后，在第 12 章追加模块完成记录。
4. 模块只有在功能、OpenAPI、数据库映射、测试、日志、审计和必要文档均完成后才能标记 `[x]`。
5. 只完成代码或只通过编译不能算模块完成。
6. 出现阻塞时，在对应模块直接写明原因和解除条件，不使用难以理解的外部编号代替说明。
7. 模块 01 到模块 16 是默认顺序，不是机械限制；可根据依赖、环境、风险和项目优先级调整，但必须先在本计划中记录原因、影响和新的当前模块，不得打乱模块边界。
8. 重大架构、数据库、安全边界或技术基线变化必须同步更新 ADR。

### 3.3 每个后端模块的统一工程流程

每个模块都在自己的章节内依次完成以下工作：

1. 阅读该模块设计和数据库设计，确定模块职责、不负责事项及与相邻模块的接口；
2. 开发前检索 GitHub、Gitee、官方文档和成熟开源项目，记录许可证、维护状态、安全风险和复用结论；
3. 明确模块 API、权限、错误码、审计事件、幂等和并发要求；
4. 先更新 OpenAPI 工作契约，再生成类型和接口；
5. 严格依据数据库设计编写 sqlc 查询、Repository 和必要的追加 Migration，不得虚构字段；
6. 实现领域、应用、Repository、HTTP 适配和必要的后台任务接口；
7. 完成单元、数据库集成、HTTP 契约、权限、并发和异常测试；
8. 执行格式化、静态检查、构建、漏洞检查并复核 Diff；
9. 更新模块文档、API 示例、状态和完成记录。

审计和后台任务虽然分别是模块 11、模块 16，但前序模块仍需定义并产生属于自己的审计事件和领域事件；模块 11、模块 16 负责完成统一查询、完整性、调度、重试和运维能力。

## 4. 项目总体状态

| 顺序 | 阶段 | 状态 | 当前说明 |
|---:|---|---|---|
| 1 | 基础准备 | `[x]` | Go、PostgreSQL、Redis、Gin、Migration、sqlc、OpenAPI 和测试基线已完成 |
| 2 | 后端系统模块开发 | `[~]` | 模块 01～05 已完成当前边界；模块 06～11 和模块 16 已完成 Windows 本地可离线验证的当前边界；真实文件数据面等待 Linux/SeaweedFS |
| 3 | 累积 API 文档编制与最终冻结 | `[~]` | 公共、模块 01～11 当前接口和模块 16 基础运维接口已写入统一接口文档；Windows 收敛后仅随明确功能变更追加 |
| 4 | 按模块接口测试 | `[ ]` | 以冻结 API 文档为唯一依据 |
| 5 | 按模块前端开发 | `[ ]` | 接口测试稳定后开始 |
| 6 | 前后端联调与端到端测试 | `[ ]` | 核心用户流程完整验证 |
| 7 | 非功能验证 | `[ ]` | 安全、性能、恢复和可观测性 |
| 8 | 发布准备与验收 | `[ ]` | 部署、升级、回滚和交付文档 |

## 5. 已完成的基础准备

基础准备不再拆成业务开发周期，只保留清晰的完成状态和证据。

| 基础事项 | 状态 | 已完成内容 | 遗留事项 |
|---|---|---|---|
| 项目规则与设计文档 | `[x]` | `AGENTS.md`、主设计、技术选型、数据库设计和 ADR 已建立 | 后续重大变化继续同步更新 |
| PostgreSQL Schema | `[x]` | 首版单文件 SQL 已生成并由项目负责人通过 Navicat 导入 PostgreSQL 18.4 | 当前手工导入 Schema 尚未登记 Goose 版本 |
| Go 后端环境 | `[x]` | Go 1.26.5、统一 `.env`、配置校验、pgx 连接池和 `dbcheck` 可用 | 非开发环境必须改用最小权限数据库账号 |
| Migration | `[x]` | Goose 首版 Migration 可在随机临时数据库执行 `up/down` 并精确清理 | 已发布后只能追加 Migration |
| SQL 代码生成 | `[x]` | sqlc 配置、查询目录、生成代码和固定命令可重复执行 | 业务查询随各模块增加 |
| HTTP 服务 | `[x]` | Gin、标准 `http.Server`、优雅关闭、存活与就绪检查可用 | 业务路由尚未开始 |
| Redis | `[x]` | go-redis 连接池、RESP2 兼容、降级健康检查和真实 Redis 6.2.6 测试通过 | 生产前规划 Redis 升级 |
| OpenAPI 工程 | `[x]` | OpenAPI 3.1、公共错误、`page/pageSize` 和 oapi-codegen 生成链路可用 | 业务契约随模块增加 |
| 公共工程能力 | `[x]` | UUIDv7 Request ID、结构化日志、错误映射、panic 恢复和超时边界可用 | 各模块持续复用并测试 |
| 自动化测试 | `[x]` | Go Test、httptest、本地 PostgreSQL 临时库、Redis 唯一键隔离和统一脚本通过 | Testcontainers 仅为未来 Docker/CI 可选方案 |
| Git 初始化 | `[x]` | `main`、`origin`、首次 README 提交和项目源码基线已建立 | 后续提交按规则同步推送至 `origin/main` |
| SeaweedFS/S3 | `[-]` | 对象存储默认实现已由 MinIO 调整为 SeaweedFS，当前仍由项目负责人明确暂缓；Windows 本地先完成可离线验证的控制面 | 真实上传闭环、Bucket、安全凭据、Multipart 兼容性、下载和 Range 必须在 SeaweedFS S3 Gateway 可用后验证 |

## 6. 后端系统模块开发

### 6.1 模块总览

模块名称严格对应开发设计文档第 6.2～6.17 节，表中顺序是默认开发顺序；实际执行可按依赖、环境和优先级调整，并在状态中留痕。

| 顺序 | 系统模块 | 状态 | 主要依赖或说明 |
|---:|---|---|---|
| 01 | 身份认证 | `[x]` | 当前确认范围已完成；MFA 明确暂缓，外部身份源待确定，统一不可篡改审计由模块 11 承接 |
| 02 | 用户管理 | `[x]` | 用户资料、生命周期、管理员操作、本人会话、并发与 Outbox 已完成 |
| 03 | 组织与空间 | `[x]` | 组织树、成员、三类空间、并发配额和 MOVE 重组计划已完成；文件内容型重组执行由模块 05/16 接入 |
| 04 | 权限与管理委派 | `[x]` | 唯一资源授权入口、管理委派、ACL 继承、最终复核和版本化缓存已完成 |
| 05 | 文件目录 | `[x]` | 目录命名空间、Folder/Document 稳定身份、列表、详情、移动、重命名和权限复核已完成当前边界 |
| 06 | 文件传输与存储 | `[~]` | 已完成 Object Storage Interface、AWS SDK for Go v2 S3 适配器、禁用模式配置、健康检查和上传控制面当前周期；完成提交、下载、Range 和真实 SeaweedFS 兼容性待后续 |
| 07 | 版本与并发 | `[~]` | 已完成版本列表、历史版本恢复和文件级租约锁基础接口；真实上传生成版本、下载和 WebDAV 锁适配待后续 |
| 08 | 共享 | `[~]` | 已完成 USER/ORGANIZATION/LINK 内部共享基础接口和权限模块共享允许来源；SPACE 引用、匿名外链和真实下载/预览待后续 |
| 09 | 回收与生命周期 | `[~]` | 已完成回收站元数据闭环、管理员过期扫描、`LIFECYCLE_PURGE` 入队编排和资料保全基础接口；真实对象存储清理、审批流与最终 `PURGED` 收敛待后续 |
| 10 | 搜索 | `[~]` | 已完成 PostgreSQL 元数据搜索基础接口、候选结果最终权限复核、降级响应和索引刷新任务入队；真实 INDEX 处理器、全文、OCR、语义搜索和外部索引服务待后续 |
| 11 | 审计 | `[~]` | 已完成 Outbox 审计消费者、审计分页查询、详情查询、摘要统计增强和哈希链完整性校验；导出、归档和告警待对象存储/后续周期 |
| 12 | 预览 | `[ ]` | 只读取已授权的指定版本，不修改原文件 |
| 13 | WebDAV兼容 | `[ ]` | 仅为接入适配器，复用统一身份、权限、文件和锁 |
| 14 | SMB迁移 | `[ ]` | 依赖目录、存储、权限映射和后台任务 |
| 15 | AI与Agent集成 | `[ ]` | 按确认的 V1.0 启用范围开发，禁止直持数据库或存储凭据 |
| 16 | 后台任务 | `[~]` | 已完成 Outbox Worker、`background_jobs` 基础调度、管理员积压统计、队列延迟摘要、健康摘要、失败原因聚合、过期租约恢复、Outbox/Job 受控重试、受控取消、批量死信和批量跳过接口；具体业务处理器、告警和更多审计消费者继续开发 |

### 6.2 模块 01：身份认证

目标：建立可靠的登录身份和会话上下文，不在本模块判断用户对文件资源的权限。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 模块调研与边界 | `[x]` | 已比较完整身份系统和 Go 安全组件，结论记录于 `docs/research/M01-身份认证模块开源方案调研.md` |
| 认证契约 | `[x]` | 工作版 OpenAPI 已定义登录、退出、刷新和会话检查接口、Cookie/Bearer 入口及稳定错误码；MFA 本轮不开放未实现路由 |
| 凭据与密码 | `[x]` | 已完成 Argon2id、12～128 字符、用户名和常见弱密码拒绝；按项目负责人最新决定取消密码历史策略，账号写入由模块 02 使用该能力 |
| 会话与 Token | `[x]` | 已完成会话创建、15 分钟 JWT、7 天 Refresh Token、单事务轮换、注销撤销、过期校验、重放和并发刷新防护 |
| 登录保护 | `[x]` | 已完成失败计数、成功后窗口重置、5 次失败锁定、单实例 IP 突发限流和 `login_attempts` 记录；MFA、LDAP/OIDC/AD 和统一审计是已接受的延期范围，不阻止本模块闭环 |
| 模块测试与文档 | `[x]` | 已覆盖成功、失败、锁定、JWT 过期、撤销、并发刷新、重放、Cookie、Origin 与 CORS 预检；模块接口已写入统一 API 接口文档并加入同步校验 |

完成标准：认证接口、OpenAPI 和统一 API 接口文档一致；会话生命周期完整；失败不降级为放行；日志和登录尝试记录不泄露密码、Token 或恢复密钥。当前范围已于 2026-08-05 达成。

### 6.3 模块 02：用户管理

目标：完成人员账号、资料、状态、系统角色和用户生命周期管理。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 用户契约与权限 | `[x]` | 已完成本人资料/会话和管理员查询、创建、更新、启停、锁定、密码重置、逻辑删除共 13 个接口；管理员入口只允许 `SYSTEM_ADMIN` |
| 用户生命周期 | `[x]` | 已实现资料、`USER`/`SYSTEM_ADMIN`、四种用户状态、终态删除、最后活动管理员保护、会话即时失效和乐观锁 |
| 组织关系协作 | `[x]` | 已冻结边界并通过 `USER_CREATED/UPDATED` 等 Outbox 提供协作入口；组织成员关系只由模块 03 写入 |
| 个人空间与交接 | `[x]` | 已确定用户创建事件触发个人空间初始化、逻辑删除不级联文件事实；实际空间建立和文件交接分别由模块 03 及文件生命周期模块完成 |
| 用户偏好 | `[x]` | 已确认收藏和最近访问属于用户侧投影，但其资源有效性与写入入口由文件目录/搜索模块实现，用户模块不越界操作资源表 |
| 模块测试与文档 | `[x]` | 单元和真实 PostgreSQL/Redis HTTP 测试覆盖幂等、分页、越权、并发更新、状态转换、会话撤销、密码重置和逻辑删除；已追加统一 API 文档 |

完成标准：用户逻辑删除不级联破坏文件、版本、授权、共享或审计；敏感字段不进入普通响应；用户状态能正确影响认证和授权。当前模块边界已于 2026-08-05 达成，跨模块资源创建和迁移由对应模块按 Outbox 契约继续实现。

### 6.4 模块 03：组织与空间

目标：建立组织树、成员关系和个人/组织/公共空间边界。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 组织与空间契约 | `[x]` | OpenAPI 已定义本人组织/个人空间、管理员组织、成员、空间和重组计划共 23 个接口，统一使用 `page/pageSize`、`rowVersion` 和稳定错误码 |
| 组织树 | `[x]` | 已实现创建、查询、修改、移动、停用、归档和逻辑删除阻断；闭包表、事务咨询锁、乐观锁、子树版本和循环关系防护保持一致 |
| 成员关系 | `[x]` | 已实现用户与组织多对多关系、`PRIMARY/MEMBER`、职务、有效期、状态、活动主职唯一约束和组织/用户安全版本递增 |
| 空间管理 | `[x]` | 创建组织时同事务建立组织空间；支持显式建立个人空间、公共空间及名称、容量、版本化 JSON 配置和状态管理 |
| 配额与重组 | `[x]` | 已实现原子配额预留/消费/释放和并发防超卖；重组计划支持幂等操作编排、校验、审批、取消及 `MOVE_NODE` 同步执行 |
| 模块测试与文档 | `[x]` | 单元和真实 PostgreSQL/Redis HTTP 测试已覆盖幂等、分页、越权、主职唯一、成员有效期、并发配额、并发移动、防环、计划流转和删除阻断；统一 API 文档及工程说明已补齐 |

完成标准：组织和空间边界清晰；本模块提供受控个人空间初始化能力，不自行实现文件访问权限计算。当前模块边界已于 2026-08-05 达成。由于数据库设计未定义个人空间默认配额，自动消费 `USER_CREATED` 的调度和可配置默认策略由模块 16 接入；涉及文件事实的合并、拆分和内容迁移执行器由模块 05/16 接入，不开放伪实现。

### 6.5 模块 04：权限与管理委派

目标：建立全系统唯一的资源授权判定入口。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 权限模型与契约 | `[x]` | OpenAPI 已完成 14 个接口，覆盖授权记录、权限查询、继承、断开继承、管理委派、撤销和特权访问确认 |
| 有效权限计算 | `[x]` | 已实现个人空间所有者、系统管理员、管理委派、用户/组织 ACL、直接/继承并集和默认拒绝的确定性顺序；V1.0 不实现显式 DENY |
| 管理委派 | `[x]` | 已实现 `SELF/SUBTREE`、七类能力、父链有效性、能力/范围/有效期上限、递归委派和父撤销后代立即失效 |
| 批量过滤与缓存 | `[x]` | 已实现最多 100 项批量最终判定和 Redis 版本化短缓存；成员、委派、授权、空间或组织版本变化立即切换 Key，Redis 故障回源 PostgreSQL |
| 最终授权复核 | `[x]` | 已形成可供 REST、搜索、分享、WebDAV、AI 和后台任务调用的应用服务入口；不存在或不确定统一拒绝，不暴露资源细节 |
| 模块测试与文档 | `[x]` | 单元、真实 PostgreSQL/Redis HTTP 和空库 Migration 测试已覆盖默认拒绝、所有者、特权确认、继承断开、委派递归/失效、ACL 撤销和缓存失效；统一 API 文档和工程说明已补齐 |

完成标准：任何入口都不能绕过本模块；不确定时拒绝或少返回；权限撤销后不会因缓存继续放行。当前模块边界已于 2026-08-05 达成。模块 08 后续把 Share 允许结果接入该入口，模块 11 消费 Outbox 建立不可篡改审计链；当前不伪造未实现的共享或审计查询能力。

### 6.6 模块 05：文件目录

目标：建立文件夹树、Document 稳定身份和统一目录命名空间。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 目录契约 | `[x]` | OpenAPI 已定义空间目录列表、文件夹/文档占位创建、移动、重命名和详情接口，统一使用 `page/pageSize`、`rowVersion`、`Idempotency-Key` 和稳定错误码 |
| 命名空间 | `[x]` | 已实现目录名称规范化、同目录唯一约束、根目录懒创建、路径缓存、深度维护和树结构边界 |
| 文件夹与文档 | `[x]` | 已实现 Folder 与 Document 共享主键的稳定身份；Document 当前版本指针保持为空，上传完成前 `availabilityStatus=BLOCKED`，不伪造存储对象或版本 |
| 移动与重命名 | `[x]` | 已实现循环移动防护、重名冲突、乐观锁、文件夹后代路径刷新和移动后空间安全纪元递增；容量增量由模块 06/07 上传和版本流程接入 |
| 列表与分页 | `[x]` | 已实现稳定排序、`page/pageSize`、类型/生命周期筛选、空根目录返回和无界查询防护 |
| 模块测试与文档 | `[x]` | 单元和真实 PostgreSQL/Redis HTTP 测试覆盖中文目录、根目录懒创建、文档占位、列表、重命名、移动、防环、幂等、Outbox 与清理约束；统一 API 文档和工程说明已补齐 |

完成标准：文件和文件夹在统一命名空间中行为一致；所有目录查询经过权限模块；数据库约束与服务规则一致。当前模块边界已于 2026-08-10 达成。真实二进制上传、对象存储、版本内容、容量扣减、扫描、预览和索引由模块 06/07/10/12/16 接入。

### 6.7 模块 06：文件传输与存储

目标：建立大文件上传、下载和对象存储数据面。

开始条件：原计划要求先搭建 SeaweedFS S3 Gateway。经 2026-08-10 确认，当前改为 Windows 本地优先推进可离线验证的 Object Storage Interface、AWS SDK for Go v2 S3 适配器、禁用模式配置和健康检查；真实上传会话、分片直传、完成提交、下载和兼容性测试仍需 SeaweedFS S3 Gateway 可用后继续。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| SeaweedFS/S3 基线 | `[~]` | 已完成项目内 Object Storage Interface、AWS SDK for Go v2 S3 适配器、禁用实现、环境变量和健康检查接入；真实 SeaweedFS S3 Gateway 连接、Bucket、安全凭据、Multipart、预签名和 Range 兼容性待环境可用后验证 |
| 传输契约 | `[~]` | 已定义创建上传会话、获取上传会话、分片预签名和取消上传接口；完成确认、下载和 Range 接口待后续周期 |
| 上传流程 | `[~]` | 已实现创建会话、S3 Multipart 初始化、配额预留、幂等、系统对象 Key、分片预签名和取消释放配额；`upload_parts` 登记、完成校验、Hash 与版本写入待后续 |
| 下载流程 | `[ ]` | 实现授权下载、短期凭证、Range、内容头和流式处理 |
| 存储对象管理 | `[ ]` | 实现系统生成对象 Key、对象事实、孤儿扫描和垃圾回收 |
| 模块测试与文档 | `[~]` | 已补充上传服务单元测试、对象存储禁用实现和预签名 URL 基础校验测试；已追加统一 API 文档；真实 SeaweedFS、HTTP 集成、重复提交、超时、断点、大文件、越权、签名过期和孤儿对象测试待后续周期补齐 |

完成标准：二进制不经过 Base64；浏览器不获得长期存储凭据；大文件不被 API 进程整体读入内存；数据库事实与对象状态可恢复一致。

### 6.8 模块 07：版本与并发

目标：建立不可变文件版本和明确的并发写入控制。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 版本与锁契约 | `[~]` | 已定义版本列表、历史版本恢复、锁查询、获取锁、心跳、释放和强制解锁接口；真实版本创建和下载接口待模块 06 完成上传闭环后继续 |
| 不可变版本 | `[~]` | 已实现历史版本读取和恢复时追加新版本并切换当前版本；上传完成创建真实版本待模块 06 后续周期 |
| 版本恢复 | `[x]` | 已按 `document_versions` 只追加约束实现恢复为新版本，不修改旧版本，要求 `Idempotency-Key` 和 `documents.row_version` |
| 并发控制 | `[~]` | 已实现文件级租约锁、续租、本人释放、系统管理员强制释放、过期清理和单调 `fencingToken`；写入提交时的 fencing 校验待上传完成/版本创建周期接入 |
| 模块测试与文档 | `[~]` | 已补充应用层单元测试、OpenAPI、统一 API 文档和轻量调研；真实 HTTP 集成、WebDAV 适配、Office/CAD 兼容和锁竞争压力测试待后续 |

完成标准：历史版本不可变；并发冲突可被客户端明确识别；锁不会永久遗留或被无权主体释放。

### 6.9 模块 08：共享

目标：实现受权限控制、可撤销、可审计的内部共享。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 共享契约 | `[~]` | 已定义 USER、ORGANIZATION、LINK 共享创建、详情、列表、打开、更新和撤销接口；目标 SPACE 共享引用待 shared_entries 周期 |
| 共享条目 | `[~]` | 已实现 `shares/share_actions` 事实写入、动作白名单、有效期、状态、LINK token 摘要和 Outbox；`shared_entries` 命名空间引用待后续 |
| 访问控制 | `[~]` | 创建共享必须具备源资源 `SHARE` 和所授予动作权限；权限模块已接入有效 USER/ORGANIZATION 共享；LINK 必须通过打开接口校验 token |
| 撤销与失效 | `[~]` | 已实现撤销、到期批量标记和共享版本递增；源资源删除导致 SOURCE_UNAVAILABLE、搜索索引更新和共享引用移除待后续 |
| 模块测试与文档 | `[~]` | 已补充应用层测试、OpenAPI、统一 API 文档和轻量调研；HTTP 集成、跨组织真实数据和过期/撤销缓存失效集成测试待后续 |

完成标准：外部匿名链接继续默认关闭；共享不能扩大创建者本身没有的权限；撤销后不能依赖旧缓存继续访问。

### 6.10 模块 09：回收与生命周期

目标：实现可恢复删除、保留、归档、冻结和合法清理。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 生命周期契约 | `[~]` | 已定义进入回收站、回收站分页列表、恢复、发起永久清理、管理员过期扫描和资料保全管理 9 个接口；保留策略、归档、冻结管理接口待后续策略周期 |
| 回收与恢复 | `[~]` | 已实现文件/文件夹进入回收站、递归标记 `TRASHED`、原位置/指定父目录恢复和名称冲突处理；真实对象状态与版本引用不在本周期伪造 |
| 保留与归档 | `[-]` | 保留策略配置、归档、冷存储状态和生命周期批量计算暂缓；默认回收保留期当前固定为 90 天 |
| 资料保全 | `[~]` | 已在永久清理前查询底层 `legal_holds` 并阻断有效资料保全；已实现创建、列表、详情和解除接口；审批流与批量保全待后续治理周期 |
| 清理流程 | `[~]` | 已实现永久清理发起：目录子树标记 `PURGING`、回收项标记 `PURGING`、写入 Outbox；已实现过期回收项扫描并入队 `LIFECYCLE_PURGE`；实际对象存储删除、引用计数和最终 `PURGED` 收敛由 Worker 后续实现 |
| 模块测试与文档 | `[~]` | 已补充应用层测试、OpenAPI、统一 API 文档和轻量调研；HTTP 集成、真实大目录、对象存储清理、资料保全审批流和 `LIFECYCLE_PURGE` 处理器测试待后续 |

完成标准：资料保全不能被普通删除或生命周期策略绕过；永久清理可追踪、可重试且不会清理仍被引用的对象。当前已达成元数据回收闭环、过期扫描入队编排和资料保全基础管理，未宣称完成真实对象清理和完整生命周期治理。

### 6.11 模块 10：搜索

目标：提供权限安全的元数据、全文和可选语义检索。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 搜索契约 | `[~]` | 已定义 `GET /api/v1/search` 和 `POST /api/v1/admin/search/index-refresh-jobs`；搜索列表统一使用 `page/pageSize` |
| 元数据搜索 | `[~]` | 已基于 PostgreSQL 查询 `namespace_entries/documents/document_index_states`，仅返回 `ACTIVE` 目录项，按匹配度、更新时间和 ID 稳定排序 |
| 全文与索引 | `[~]` | 已完成管理员索引刷新任务入队：标记 `document_index_states.status=PENDING` 并写入 `INDEX` 后台任务；真实 `INDEX` 处理器、全文内容、OCR、外部搜索服务和重建暂缓；当前搜索响应 `degraded=true` 明确降级 |
| 权限安全过滤 | `[~]` | 已在返回前逐项调用权限模块 `READ_METADATA` 最终复核；无权候选被过滤，权限服务错误不吞掉 |
| 语义检索接口 | `[-]` | pgvector/外部向量后端暂缓，不在未启用时影响核心搜索 |
| 模块测试与文档 | `[~]` | 已补充应用层测试、OpenAPI、统一 API 文档和专项调研；测试覆盖搜索权限过滤、参数校验、索引刷新管理员限制、重复文档拒绝、缺失文档单项失败和入队成功；HTTP 集成、权限撤销集成、索引延迟和大数据量性能测试待后续 |

完成标准：搜索故障不破坏业务事实；索引可重建；任何情况下都不得返回无权资源。当前周期完成元数据搜索基础入口和索引刷新任务入队，未宣称完成真实全文索引处理器、OCR、语义检索和独立搜索引擎。

### 6.12 模块 11：审计

目标：汇总全系统安全和业务事件，提供不可变、可验证的审计能力。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 审计事件模型 | `[x]` | 已统一 Outbox 事件到 `audit_events` 的主体、动作、资源、结果、Request ID、风险级别、metadata 和必要资源 ID 映射；不凭空补造数据库字段 |
| 可靠写入 | `[x]` | 已将用户、组织、权限和文件目录模块当前 Outbox 事件注册为审计消费者；业务事实仍先进入 Outbox，Worker 再可靠写入审计事实，失败按后台任务重试/死信处理 |
| 完整性保护 | `[x]` | 已基于数据库只追加触发器和 Go 侧 SHA-256 Hash Chain 建立高风险事件链，写入时行锁串行化链头，提供链完整性校验 |
| 查询与导出 | `[~]` | 已实现 `SYSTEM_ADMIN` 受控摘要统计、分页查询、筛选、详情和完整性状态查询；审计导出、脱敏导出文件和归档下载待对象存储/归档周期 |
| 归档与告警 | `[-]` | 对象存储集群尚未搭建，归档、WORM、批次锚定和安全告警暂缓，不阻塞当前基础审计查询闭环 |
| 模块测试与文档 | `[~]` | 已覆盖 Outbox 映射、Request ID 兜底、哈希篡改敏感性、摘要统计管理员限制和日期范围校验，并同步 OpenAPI、统一 API 文档和专项调研；导出脱敏、归档、告警和真实链篡改集成测试后续补充 |

完成标准：普通业务接口不能更新或删除审计记录；审计查询自身可追踪；敏感秘密不进入审计内容。

### 6.13 模块 12：预览

目标：安全生成并提供文件预览、缩略图和文本内容。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 预览契约 | `[ ]` | 定义预览状态、获取、重试、缩略图和文本结果接口 |
| 类型识别 | `[ ]` | 基于内容识别文件类型，不仅信任扩展名或客户端声明 |
| 预览处理 | `[ ]` | 实现 PDF、图片、文本、Markdown 和 Office 适配边界，预留 CAD 接口 |
| 隔离与限制 | `[ ]` | 对不可信文件限制 CPU、内存、磁盘、网络和执行时间 |
| 缓存与重试 | `[ ]` | 实现指定版本预览缓存、失败状态和有限重试 |
| 模块测试与文档 | `[ ]` | 覆盖恶意文件、超大文件、处理超时、版本权限、缓存失效和降级 |

完成标准：预览只读取经授权的指定版本，不修改原文件；预览失败不影响原文件下载和业务事实。

### 6.14 模块 13：WebDAV兼容

目标：以接入适配器形式提供 WebDAV，不建立第二套文件和权限模型。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 协议调研与范围 | `[ ]` | 调研成熟 WebDAV 库、客户端兼容差异和许可证，确认 V1.0 方法范围 |
| 方法与路径映射 | `[ ]` | 把 WebDAV 方法、路径和状态码映射到统一应用服务 |
| 身份与权限 | `[ ]` | 复用统一认证、资源权限和审计，不建立旁路账号或授权规则 |
| 锁与并发 | `[ ]` | 将 WebDAV 锁语义映射到版本与并发模块 |
| 模块测试与文档 | `[ ]` | 覆盖目标客户端、路径编码、大文件限制、越权、锁冲突和错误映射 |

完成标准：WebDAV 不直接访问其他模块数据表；所有写入遵守版本、权限、审计和存储规则。

### 6.15 模块 14：SMB迁移

目标：提供可中断、可恢复、可核验的 SMB 文件迁移能力。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| 迁移方案调研 | `[ ]` | 比较成熟 SMB 客户端和迁移工具，确认协议、许可证、安全和平台兼容性 |
| 扫描与映射 | `[ ]` | 实现目录扫描、元数据提取、用户映射和 ACL 映射预检 |
| 初次迁移 | `[ ]` | 实现目录、文件、版本和必要权限的受控导入 |
| 增量与切换 | `[ ]` | 实现增量扫描、断点续传、冲突策略、切换和核验 |
| 报告与恢复 | `[ ]` | 实现进度、失败重试、取消、错误报告和幂等恢复 |
| 模块测试与文档 | `[ ]` | 覆盖路径攻击、非法名称、权限映射失败、网络中断、重复导入和数据核验 |

完成标准：迁移失败不会产生不可识别的半成品；每个来源项有明确结果；切换前可以核对数量、大小和 Hash。

### 6.16 模块 15：AI与Agent集成

目标：在统一权限、审计和资源限制下提供可选 AI/RAG 与 Agent 能力。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| V1.0 范围确认 | `[ ]` | 开发前由项目负责人确认 AI、RAG、Dify、MCP 和 Agent Action 的实际启用范围 |
| 模型与任务适配 | `[ ]` | 实现模型适配、任务状态、超时、限额和故障降级 |
| 文档处理与 RAG | `[ ]` | 实现 OCR、文本提取、切块、Embedding、引用和可重建索引 |
| Agent Gateway | `[ ]` | 实现 Tool/MCP、动作确认、幂等和最小权限调用 |
| 配置与审计 | `[ ]` | 管理模型、Workflow、Prompt 修订，记录 AI 任务和写操作审计 |
| 模块测试与文档 | `[ ]` | 覆盖提示注入、越权检索、恶意输出、确认绕过、超时、限额和供应商故障 |

完成标准：模型和 Agent 不直接持有数据库或对象存储长期凭据；AI 输出按不可信输入处理；停用 AI 不影响核心文件能力。

### 6.17 模块 16：后台任务

目标：统一承载可靠异步处理、重试、死信和运维控制。

| 工程任务 | 状态 | 具体内容 |
|---|---|---|
| Outbox 与 Worker | `[x]` | 已完成事务外盒 Worker 框架、按已注册事件类型认领、`FOR UPDATE SKIP LOCKED`、租约、并发轮询、处理器超时和优雅关闭 |
| 任务调度 | `[x]` | 已基于 `background_jobs` 建立通用入队、认领、执行、心跳续租和状态机；统一索引、AI、病毒扫描、预览、生命周期和审计归档处理器后续接入 |
| 重试与死信 | `[x]` | Outbox 与 Job 均已实现可重试/永久失败分类、指数退避、失败摘要、重试耗尽转 `DEAD`，并提供 `FAILED/DEAD` 单项受控重试 API |
| 幂等与恢复 | `[~]` | Outbox/Job 已通过去重键、注册处理器过滤、状态机、领取端租约过期恢复和管理员过期租约恢复接口支持重复领取安全；具体业务处理器幂等策略随各消费者接入 |
| 运维与观测 | `[~]` | 已提供管理员分页查询 Outbox/Job 积压、失败原因、状态统计、队列延迟摘要、健康摘要、失败原因 Top 20 聚合、过期租约恢复、Outbox/Job 单项与批量重试、单项取消、批量取消、批量死信和批量跳过接口；指标和告警后续补齐 |
| 模块测试与文档 | `[~]` | 已覆盖 Outbox Worker、Job Runner、后台运维应用层与真实 PostgreSQL/Redis HTTP 流程、OpenAPI、统一 API 文档和模块说明；并发压力、崩溃恢复、指标和告警后续补充 |

完成标准：PostgreSQL 业务事实、Outbox 和 Job 事实保持可追踪；Redis 不是任务事实唯一来源；Worker 可安全停止和恢复，不泄漏 Goroutine；管理员可查询积压、查看状态统计、队列延迟和健康摘要，定位失败原因、主动恢复过期租约、受控重试失败项、受控取消未完成任务，并对确认无法继续自动处理的后台任务执行批量死信或跳过。当前已完成后台任务基础调度、统计、队列延迟摘要、健康摘要、失败原因聚合、过期租约恢复、Outbox/Job 重试、取消、死信和批量运维接口周期，模块 16 尚未整体完成。

### 6.18 后端阶段完成标准

- 16 个系统模块均标记为 `[x]`，或有项目负责人明确接受的延期范围；
- 项目级开源参考持续维护；高复杂度、高风险或重大选型模块具有专项调研，常规模块不强制重复调研；所有模块均有工作版 OpenAPI、实现、测试和完成记录；
- 所有 SQL 和持久化映射均对应数据库设计说明，不存在虚构字段；
- REST、WebDAV、搜索、共享、AI 和后台任务使用统一身份、权限和审计语义；
- 所有列表接口只使用 `page`、`pageSize` 并保持稳定排序；
- 全量格式化、静态检查、测试、构建和漏洞扫描通过。

## 7. 累积 API 文档编制与最终冻结

状态：`[~]` 进行中。

后端开发期间，每个模块先维护 OpenAPI，再在模块完成时立即追加到统一可读接口文档。16 个后端模块完成后只进行全量汇总、校验和冻结，不再从零补写文档。

| 工作项 | 状态 | 具体内容 |
|---|---|---|
| 按模块追加接口 | `[~]` | 公共、模块 01 和模块 02 已完成；后续逐模块追加路由、Operation ID、权限、参数、响应、错误码和副作用 |
| 校验实现一致性 | `[~]` | 已建立路径和 Operation ID 同步校验；后续继续检查 Schema、状态码、Content-Type 和实现偏差 |
| 维护可读文档 | `[~]` | 统一维护 `docs/File-Workshop-V1.0-API接口文档.md`，OpenAPI 仍是机器契约权威来源 |
| 补齐场景示例 | `[~]` | 已补健康检查、身份认证、用户管理、分页、幂等、并发、状态和错误示例；后续随模块追加 |
| 冻结测试版本 | `[ ]` | 记录版本、日期、校验结果和变更规则，作为接口测试唯一依据 |

计划交付：

- `backend/api/openapi.yaml`：持续维护并最终冻结的机器可读契约；
- `docs/File-Workshop-V1.0-API接口文档.md`：按模块累计的可读接口文档；
- `docs/api/examples/`：必要请求和响应示例；
- 固定文档生成、校验和破坏性变更检查命令。

## 8. 按模块执行接口测试

状态：`[ ]` 未开始。

接口测试依据第 7 章累计 API 接口文档和对应 OpenAPI，并按系统模块组织，不再按技术类型拆散；正式发布测试使用最终冻结版本。

| 顺序 | 测试模块 | 状态 | 重点测试 |
|---:|---|---|---|
| 01 | 身份认证 | `[ ]` | 登录、刷新、注销、MFA、限流、撤销、过期、重放 |
| 02 | 用户管理 | `[ ]` | 生命周期、状态、角色、越权、并发更新 |
| 03 | 组织与空间 | `[ ]` | 组织树、成员、空间、配额、跨组织隔离 |
| 04 | 权限与管理委派 | `[ ]` | 允许、拒绝、继承、委派、撤销、缓存失效 |
| 05 | 文件目录 | `[ ]` | 创建、列表、移动、重命名、冲突、分页和路径边界 |
| 06 | 文件传输与存储 | `[ ]` | 分片、断点、重复完成、下载、Range、大文件和越权 |
| 07 | 版本与并发 | `[ ]` | 版本、恢复、乐观并发、锁、心跳和过期 |
| 08 | 共享 | `[ ]` | 创建、访问、到期、撤销、枚举和权限收回 |
| 09 | 回收与生命周期 | `[ ]` | 回收、恢复、永久清理、保留和冻结 |
| 10 | 搜索 | `[ ]` | 筛选、分页、索引延迟、权限后过滤和降级 |
| 11 | 审计 | `[~]` | 已覆盖基础写入、查询、完整性和特权访问；导出、脱敏和归档待对象存储周期 |
| 12 | 预览 | `[ ]` | 类型、状态、重试、权限、恶意文件和资源限制 |
| 13 | WebDAV兼容 | `[ ]` | 方法、路径、客户端兼容、权限和锁 |
| 14 | SMB迁移 | `[ ]` | 扫描、映射、断点、重试、冲突、报告和核验 |
| 15 | AI与Agent集成 | `[ ]` | 引用、确认、越权、提示注入、限额和降级 |
| 16 | 后台任务 | `[ ]` | 认领、重试、死信、重复消费、崩溃恢复和积压 |
| 全局 | 公共 API 规范 | `[ ]` | `page/pageSize`、错误格式、Request ID、幂等键、超时、注入和超大输入 |
| 回归 | 缺陷修复与全量回归 | `[ ]` | 缺陷闭环、受影响模块重测和接口测试报告 |

完成标准：冻结文档中的每个接口均有测试或书面豁免；权限、分页、幂等、并发和错误响应测试通过；测试数据可隔离并清理。

## 9. 按模块开发前端

状态：`[ ]` 未开始。

前端开始前先检查当前 `frontend/` 开源模板的许可证、维护状态、依赖安全和技术栈适配成本。前端公共外壳、OpenAPI 生成客户端、设计令牌和明暗主题只建立一次，业务页面仍按系统模块组织。

| 顺序 | 前端模块 | 状态 | 主要页面或交互 |
|---:|---|---|---|
| 基础 | 前端工程与公共外壳 | `[ ]` | 模板决策、严格 TypeScript、路由、API 客户端、布局、设计令牌和全局明暗切换 |
| 01 | 身份认证 | `[ ]` | 登录、会话过期、MFA、错误和提交状态 |
| 02 | 用户管理 | `[ ]` | 用户资料、账号状态、角色和管理操作 |
| 03 | 组织与空间 | `[ ]` | 组织树、成员、空间、配额和重组 |
| 04 | 权限与管理委派 | `[ ]` | 授权、继承、委派、撤销和敏感操作确认 |
| 05 | 文件目录 | `[ ]` | 空间导航、目录树、文件列表、筛选、排序、分页和移动 |
| 06 | 文件传输与存储 | `[ ]` | 上传队列、暂停、恢复、取消、重试、刷新恢复和下载 |
| 07 | 版本与并发 | `[ ]` | 版本历史、恢复、锁状态和冲突反馈 |
| 08 | 共享 | `[ ]` | 分享创建、目标选择、有效期、权限和撤销 |
| 09 | 回收与生命周期 | `[ ]` | 回收站、恢复、永久删除、保留和冻结提示 |
| 10 | 搜索 | `[ ]` | 搜索、筛选、防抖、分页、权限结果和降级状态 |
| 11 | 审计 | `[~]` | 已覆盖审计筛选、详情和完整性状态；导出待对象存储周期 |
| 12 | 预览 | `[ ]` | 图片、PDF、文本、Markdown、Office 状态和失败重试 |
| 13 | WebDAV兼容 | `[ ]` | 连接说明、兼容状态和限制提示；不复制文件管理页面 |
| 14 | SMB迁移 | `[ ]` | 任务创建、映射、进度、错误报告、重试和核验 |
| 15 | AI与Agent集成 | `[ ]` | AI 检索、引用、动作确认、权限提示和故障降级 |
| 16 | 后台任务 | `[ ]` | 任务状态、积压、失败、重试、取消和死信管理 |
| 收尾 | 前端测试与收敛 | `[ ]` | Vitest、组件测试、无障碍、构建和关键 Playwright 流程 |

所有页面必须支持明暗主题，并处理加载、空数据、错误、无权限和部分降级状态。API DTO 和客户端从冻结 OpenAPI 生成，不手工重复维护。

## 10. 联调、非功能验证与发布

### 10.1 前后端联调与端到端测试

状态：`[ ]` 未开始。

- 建立本地联调环境，统一代理、CORS、Cookie、TLS/域名策略和测试数据；
- 按系统模块组织联调，顺序根据依赖和风险安排，再串联登录、浏览、上传、版本、权限、共享、搜索和回收站等关键旅程；
- 使用 Playwright 覆盖关键用户流程；
- 通过 Request ID、结构化日志和审计定位失败；
- 修复缺陷后同步 OpenAPI、模块测试和完成记录。

### 10.2 非功能验证

状态：`[ ]` 未开始。

- 安全：身份、权限、会话、上传、注入、密钥、依赖和供应链；
- 性能：数据库执行计划、连接池、分页、上传下载、并发和容量；
- 稳定性：依赖中断、Worker 重启、重复投递、资源泄漏和优雅关闭；
- 恢复：PostgreSQL、对象存储和配置备份恢复，记录 RPO/RTO；
- 可观测性：日志、指标、追踪、告警和敏感信息检查；
- 兼容性：目标浏览器、分辨率、键盘操作、明暗主题和基础无障碍。

### 10.3 发布准备与验收

状态：`[ ]` 未开始。

- 编写部署、配置、健康检查和密钥管理说明；
- 演练全新部署、Migration 升级、失败前滚/回滚和数据校验；
- 编写备份恢复、告警、任务重放、存储和数据库故障手册；
- 生成用户与管理员文档；
- 生成可追溯制品、校验值、SBOM、已知问题和验收报告。

## 11. 风险和待确认事项

| 事项 | 状态 | 处理方式 |
|---|---|---|
| 本地 PostgreSQL 使用高权限 `root` 和默认 `postgres` 数据库 | 已接受，仅限本地开发 | 测试、预发布和生产必须重新评审最小权限 |
| 当前 Navicat 手工导入 Schema 未登记 Goose 版本 | 部分解决 | 不自动修改现有库；重建或登记基线前必须先完成结构比对并获得授权 |
| 当前项目源码尚未建立 Git 基线提交 | 待授权 | 不自动提交或推送；获得项目负责人明确授权后处理 |
| SeaweedFS/S3 尚未搭建 | 暂缓 | 不影响模块 01～05 和模块 06 可离线验证的控制面；真实上传闭环、下载、Range 和兼容性验收前必须完成 |
| Windows 本地阶段继续扩展边缘控制面收益下降 | 已收敛 | 详见 `docs/File-Workshop-V1.0-Windows本地阶段收敛说明.md`；除缺陷修复、文档校对和轻量验证外，不再主动新增不依赖对象存储的边缘接口 |
| Redis 6.2.6 不在当前 go-redis 官方主要支持矩阵 | 已验证，仍需升级规划 | 已固定 RESP2 并通过真实测试；生产前规划升级 |
| 当前前端开源模板与冻结技术基线可能不一致 | 待评估 | 前端开发前完成许可证、安全和适配评估，必要时更新 ADR |
| WebDAV、SMB、AI/Agent 范围较大 | 待逐模块确认 | 进入模块 13～15 前确认 V1.0 最小范围，不静默删减或扩张 |

## 12. 完成记录

### 12.1 基础准备完成记录

| 完成事项 | 完成日期 | 交付物和验证证据 | 遗留事项 |
|---|---|---|---|
| 项目规则、设计文档和原开发计划 | 2026-08-05 | `AGENTS.md`、主设计、技术选型、数据库设计和 ADR 可定位 | 继续随项目演进维护 |
| PostgreSQL 首版 Schema | 2026-08-05 | 单文件 SQL 已由项目负责人通过 Navicat 导入 PostgreSQL 18.4，后端连接检查通过 | 手工导入库未登记 Goose 版本 |
| Go 配置与 PostgreSQL 连接池 | 2026-08-05 | `.env`、配置校验、pgxpool、`dbcheck`、单元测试和漏洞扫描通过 | 非开发环境最小权限待实施 |
| Goose Migration | 2026-08-05 | 空临时数据库 `up`、Schema 检查、`down` 和精确清理通过 | 首版没有上一发布版本升级路径 |
| sqlc | 2026-08-05 | 配置、健康查询、生成代码和重复生成一致性检查通过 | 业务查询随模块增加 |
| Gin HTTP 服务 | 2026-08-05 | 服务启动、存活/就绪接口、统一错误、Request ID、日志、panic 恢复和优雅关闭测试通过 | 业务接口未开始 |
| Redis | 2026-08-05 | Redis 6.2.6 真实 PING、随机唯一键读写和精确清理通过 | 生产升级规划待处理 |
| OpenAPI 工程 | 2026-08-05 | OpenAPI 3.1、oapi-codegen、公共错误、分页和 Bearer 组件生成校验通过 | 业务契约随模块增加 |
| 自动化验证 | 2026-08-05 | 生成、`gofmt`、`go mod verify`、`go vet`、`go test`、构建和 govulncheck 完成；可达漏洞为 0 | 本地集成测试依赖 PostgreSQL/Redis 可用 |
| Go 版本统一 | 2026-08-05 | 官方下载清单、本机命令行、`go.mod`、README 和验证脚本统一为 Go 1.26.5 | 以后按升级周期处理新补丁版本 |
| Git 初始化与源码基线 | 2026-08-05 | `main`、`origin`、首次 README 提交及项目源码基线已完成 | 后续提交按规则同步推送至 `origin/main` |
| 开发计划按系统模块重构 | 2026-08-05 | 本文档 V2.0 严格映射设计文档第 6.2～6.17 节，工程任务归入对应模块 | 后续按模块实时更新状态 |

### 12.2 系统模块与开发周期完成记录

每完成一个模块或边界明确的开发周期就在本表追加一行，不覆盖历史记录；周期完成不等于整个模块已经完成。

| 系统模块 | 完成日期 | 实际交付物 | 测试和验证 | 遗留事项 |
|---|---|---|---|---|
| 模块 01：身份认证 | 2026-08-05 | 登录、会话检查、JWT、Refresh Token 轮换/重放撤销、退出、密码哈希与策略、失败锁定、IP 突发限流、OpenAPI、统一 API 接口文档和工程说明 | `verify.ps1` 与真实 PostgreSQL/Redis 集成测试通过；覆盖登录、锁定、轮换、并发、重放、撤销、退出、Cookie、Origin 和 CORS；可达漏洞 0 | MFA 按确认方案暂缓；LDAP/OIDC/AD 待身份源；不可篡改统一审计由模块 11 接入；密码历史策略已取消 |
| 模块 02：用户管理 | 2026-08-05 | 本人资料/会话、管理员用户 CRUD、创建幂等、系统角色、启停锁定、逻辑删除、密码重置、乐观锁、会话失效、Security Version、Outbox、OpenAPI 和统一 API 文档 | 单元测试和真实 PostgreSQL/Redis HTTP 流程通过；覆盖普通用户越权、同键重放/冲突、唯一冲突、并发更新、状态变更、密码重置、会话撤销和逻辑删除 | 组织成员与个人空间由模块 03 消费用户事件；收藏/最近访问和离职文件迁移由对应资源模块实现；强审计链由模块 11 消费 Outbox |
| 模块 03：组织与空间 | 2026-08-05 | 组织闭包树、成员关系、个人/组织/公共空间、原子配额、组织重组计划、Outbox、OpenAPI、统一 API 文档和模块工程说明 | 单元测试和真实 PostgreSQL/Redis HTTP 流程通过；覆盖幂等、分页、管理员边界、主职唯一和有效期、配额并发防超卖、组织并发移动与防环、MOVE 计划执行和删除阻断 | 个人空间默认策略与 `USER_CREATED` 自动调度由模块 16 接入；文件内容型重组由模块 05/16 实现；资源权限由模块 04 统一计算 |
| 模块 04：权限与管理委派 | 2026-08-05 | 管理委派、递归委派与失效、用户/组织 ACL、Space/Folder/Document 继承与断开、单项/批量最终判定、版本化 Redis 缓存、安全版本、Outbox、OpenAPI、统一 API 文档、工程说明及 `00003` 触发器修复 | `verify.ps1` 和真实 PostgreSQL/Redis/Migration 集成测试通过；覆盖默认拒绝、个人所有者、系统管理员原因与二次确认、子树委派、能力上限、父撤销、直接/继承授权、BREAK、撤销和缓存立即失效 | Share 允许源由模块 08 接入；不可篡改审计查询和拒绝/特权访问链由模块 11 消费 Outbox；文件操作入口由模块 05 起强制调用本模块 |
| 模块 05：文件目录 | 2026-08-10 | 统一命名空间、根目录懒创建、Folder/Document 稳定身份、Document 占位、目录列表、详情、重命名、移动、防环、权限最终复核、Outbox、OpenAPI、统一 API 文档和模块工程说明 | `go test ./...` 与 `FILE_WORKSHOP_RUN_INTEGRATION=1 go test ./tests/integration -run TestFilesHTTPDirectoryLifecycle -count=1` 通过；覆盖真实 PostgreSQL/Redis 下的公共空间、中文目录、文档占位、分页列表、乐观锁重命名、移动和循环移动拒绝 | 模块 06 暂缓至 SeaweedFS/S3 可用；真实上传下载、对象 Key、版本内容、容量扣减、扫描、预览、搜索索引和回收生命周期由后续模块接入 |
| 模块 16：后台任务基础周期 | 2026-08-10 | `cmd/worker`、Outbox Runner、处理器注册、PostgreSQL 领取/发布/失败/死信/续租 SQL、配置项、单元测试、真实 PostgreSQL 集成测试和专项调研 | `go test ./internal/modules/background/...` 与 `FILE_WORKSHOP_RUN_INTEGRATION=1 go test ./tests/integration -run TestBackgroundWorkerOutboxLifecycle -count=1` 通过；覆盖支持事件发布、未注册事件保留、可重试失败和重试耗尽死信 | 已由后续“基础调度与运维接口周期”和“受控取消接口周期”补齐 `background_jobs` 基础调度和单项取消 API；具体业务处理器、批量处理、告警和审计消费者继续 |
| 模块 16：后台任务基础调度与运维接口周期 | 2026-08-10 | `background_jobs` 通用入队/认领/执行/续租/成功/失败/死信框架，Outbox/Job 管理员分页查询，`FAILED/DEAD` 单项受控重试接口，OpenAPI、sqlc、统一 API 文档、模块说明和真实 HTTP 集成测试 | `go test ./internal/modules/background/... ./internal/app ./internal/platform/httpserver ./tests/integration` 与 `FILE_WORKSHOP_RUN_INTEGRATION=1 go test ./tests/integration -run TestBackgroundAdministrationHTTPWorkflow -count=1 -v` 通过；覆盖管理员查询/重试、普通用户拒绝、真实 PostgreSQL/Redis 连接 | 已由后续“后台任务受控取消接口周期”补齐单项取消；模块 16 尚未整体完成，业务处理器、批量处理、积压指标、告警、审计消费者和崩溃恢复压力测试后续继续 |
| 模块 16：后台任务受控取消接口周期 | 2026-08-10 | 新增 `cancelBackgroundJob` 管理员接口，支持按 `rowVersion/reason` 将 `PENDING/FAILED/DEAD` 后台任务转为 `CANCELLED`，拒绝取消正在执行或已终结任务；同步 OpenAPI、sqlc、统一 API 文档和模块说明 | `go test ./...` 通过；应用层测试覆盖系统管理员取消、普通用户拒绝和非法输入 | 模块 16 尚未整体完成；批量重试/批量死信处理、业务处理器、积压指标、告警和崩溃恢复压力测试后续继续 |
| 模块 16：后台任务积压统计与批量运维接口周期 | 2026-08-10 | 新增 `getBackgroundAdministrationSummary`、`batchRetryBackgroundJobs`、`batchCancelBackgroundJobs` 3 个管理员接口；支持 Outbox/Job 状态统计，以及最多 50 个后台任务的批量重试或批量取消，单项失败以明细返回 | `go test ./...` 通过；应用层测试覆盖统计、批量重试部分成功/失败、重复 ID 输入拒绝 | 模块 16 尚未整体完成；批量死信处理、具体业务处理器、指标告警和崩溃恢复压力测试后续继续 |
| 模块 16：后台任务死信与跳过批量运维接口周期 | 2026-08-14 | 新增 `batchMarkBackgroundJobsDead` 和 `batchSkipBackgroundJobs` 2 个管理员接口；支持最多 50 个后台任务批量转 `DEAD` 或 `SKIPPED`，仅对允许状态成功，单项失败以明细返回；不跳过 Outbox 业务事件 | `go test ./internal/modules/background/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖批量死信部分成功/失败、批量跳过成功和普通用户拒绝 | 模块 16 尚未整体完成；具体业务处理器、指标告警、崩溃恢复压力测试和长期运行稳定性测试后续继续 |
| 模块 16：后台任务失败原因聚合接口周期 | 2026-08-14 | 新增 `getBackgroundFailureSummary` 管理员接口；只读聚合 Outbox 与 Job 中 `FAILED/DEAD` 且存在 `last_error_code` 的 Top 20 失败原因，返回数量和最近更新时间；不新增分页、不改数据库结构 | `go test ./internal/modules/background/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖管理员失败摘要查询和普通用户拒绝 | 模块 16 尚未整体完成；具体业务处理器、指标告警、崩溃恢复压力测试、长期运行稳定性测试和外部告警集成后续继续 |
| 模块 16：后台任务过期租约恢复接口周期 | 2026-08-14 | 新增 `recoverExpiredBackgroundLeases` 管理员接口；单 SQL 原子收敛 Outbox 与 Job 中 `PROCESSING` 且 `lease_until` 已过期的项目，未耗尽次数转 `FAILED` 并立即可重试，耗尽次数转 `DEAD`；请求 `batchSize` 默认 100、最大 200，必须填写原因；不改数据库结构 | `go test ./internal/modules/background/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖默认批量大小、恢复摘要、非法输入和普通用户拒绝 | 模块 16 尚未整体完成；具体业务处理器、指标告警、崩溃恢复压力测试、长期运行稳定性测试和外部告警集成后续继续 |
| 模块 16：Outbox 批量重试接口周期 | 2026-08-14 | 新增 `batchRetryBackgroundOutboxEvents` 管理员接口；支持最多 50 个 Outbox 事件按 `outboxEventId/rowVersion` 批量重试，单项失败返回明细，不影响同批其它事件；复用 `FAILED/DEAD` 到 `PENDING` 的受控重试语义；不改数据库结构 | `go test ./internal/modules/background/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖批量重试部分成功/失败、重复 ID 输入拒绝和 Outbox 专属错误文案 | 模块 16 尚未整体完成；具体业务处理器、指标告警、崩溃恢复压力测试、长期运行稳定性测试和外部告警集成后续继续 |
| 模块 16：后台任务队列延迟摘要接口周期 | 2026-08-14 | 新增 `getBackgroundQueueLagSummary` 管理员只读接口；统计 Outbox 与 Job 中已到期 `PENDING`、已到期 `FAILED`、租约过期 `PROCESSING` 和最早到期时间；不修改任务状态、不新增分页、不改数据库结构 | `go test ./internal/modules/background/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖管理员查询、普通用户拒绝和查询时间注入 | 模块 16 尚未整体完成；具体业务处理器、指标告警、崩溃恢复压力测试、长期运行稳定性测试和外部告警集成后续继续 |
| 模块 16：后台任务健康摘要接口周期 | 2026-08-14 | 新增 `getBackgroundHealthSummary` 管理员只读接口；基于 Outbox/Job 状态统计与队列延迟事实返回 `OK/ATTENTION_REQUIRED` 和关注信号；死信、到期失败和过期租约触发关注，普通到期待处理仅作为 `INFO`；不修改任务状态、不新增数据库结构 | `go test ./internal/modules/background/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖健康状态计算、信号生成、普通用户拒绝和查询时间注入 | 模块 16 尚未整体完成；真实外部告警集成、指标暴露、具体业务处理器、崩溃恢复压力测试和长期运行稳定性测试后续继续 |
| Windows 本地阶段收敛说明 | 2026-08-14 | 新增 `docs/File-Workshop-V1.0-Windows本地阶段收敛说明.md`，明确 Windows 本地已完成能力、暂缓原因、不可硬做事项、剩余收尾项和 Linux/SeaweedFS 阶段启动顺序 | `git diff --check` 通过；本周期为文档收敛，无代码变更 | Windows 本地后续只建议做缺陷修复、文档校对、轻量验证和环境启动清单；不再主动扩展边缘控制面 |
| 模块 11：审计基础查询与完整性周期 | 2026-08-10 | 审计模块 domain/application/repository/transport、OpenAPI 契约、sqlc 查询、Outbox 审计消费者、Worker 接入、`GET /audit/events`、`GET /audit/events/{auditEventId}`、`GET /audit/integrity`、`POST /audit/integrity/verify` 和统一 API 文档 | `go test ./internal/modules/audit/... ./...` 通过；覆盖 Outbox 到审计事件映射、Request ID 兜底、风险分级、哈希稳定性和篡改敏感性；全量后端测试通过 | 对象存储尚未搭建，审计导出、归档、WORM、批次锚定、告警和真实数据库篡改集成测试后续补充；当前仅系统管理员可查询/校验 |
| 模块 11：审计摘要统计接口周期 | 2026-08-14 | 新增 `GET /api/v1/audit/summary`，按 `dateFrom/dateTo` 只读统计审计事件总数、风险等级、执行结果、主体类型和哈希链状态；补充 `docs/research/M11-审计模块开源方案调研.md`，同步 OpenAPI、统一 API 文档和主设计文档 | `go test ./internal/modules/audit/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖系统管理员摘要查询、普通用户拒绝和倒置日期范围拒绝 | 模块 11 尚未整体完成；审计导出、脱敏导出文件、归档、WORM、批次锚定、告警、真实数据库篡改集成和导出文件权限控制待对象存储/归档周期 |
| 模块 11：审计摘要统计增强周期 | 2026-08-14 | 增强 `GET /api/v1/audit/summary` 响应，追加事件类型 Top 20、资源类型 Top 20 和失败码 Top 20 统计；均来自 `audit_events`，固定数量上限，不新增分页、不改数据库结构 | `go test ./internal/modules/audit/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖系统管理员摘要增强字段透出和普通用户拒绝 | 模块 11 尚未整体完成；审计导出、脱敏导出文件、归档、WORM、批次锚定、告警、真实数据库篡改集成和导出文件权限控制待对象存储/归档周期 |
| 模块 06：对象存储适配基线周期 | 2026-08-10 | `internal/platform/objectstorage` 项目内接口、禁用实现、AWS SDK for Go v2 S3 适配器、对象存储环境变量、启用时健康检查和专项调研记录 | `go test ./internal/platform/objectstorage ./internal/platform/config ./internal/app` 通过；覆盖禁用实现返回 `ErrDisabled` 和预签名 URL 基础校验 | SeaweedFS S3 Gateway 尚未搭建，未验证真实 Bucket、Multipart、预签名上传/下载、Range、对象元数据和 S3 错误映射；上传 REST API 与 `upload_sessions` 事务下周期继续 |
| 模块 06：上传控制面周期 | 2026-08-10 | OpenAPI 上传控制面契约、sqlc 上传会话查询、`internal/modules/uploads` 领域/应用/Repository/HTTP 适配、创建上传会话、获取会话、分片预签名、取消会话、配额预留释放、幂等记录、Outbox 事件和统一 API 文档 | `go test ./...` 通过；上传应用层测试覆盖对象存储禁用不落库、正常创建会话生成系统对象 Key 和预期分片数；全量后端测试通过 | 模块 06 尚未整体完成；SeaweedFS S3 Gateway 未搭建，未做真实 Multipart/预签名兼容性、`upload_parts` 登记、完成提交、Hash 校验、`storage_objects/document_versions` 写入、下载、Range 和孤儿对象清理 |
| 模块 07：版本与并发基础接口周期 | 2026-08-10 | OpenAPI 版本与锁契约、sqlc 查询、`internal/modules/versions` 领域/应用/Repository/HTTP 适配、版本列表、历史版本恢复、文档锁查询、获取、续租、释放、系统管理员强制释放、幂等记录、Outbox 事件和统一 API 文档 | `go test ./...` 通过；应用层测试覆盖获取锁一次性 token/fencingToken、已有活动锁拒绝、恢复历史版本创建新版本并切换当前版本 | 模块 07 尚未整体完成；真实上传生成版本、下载、WebDAV 锁适配、Office/CAD 兼容和锁竞争压力测试待后续 |
| 模块 08：共享基础接口周期 | 2026-08-10 | OpenAPI 共享契约、sqlc 查询、`internal/modules/shares` 领域/应用/Repository/HTTP 适配、创建 USER/ORGANIZATION/LINK 共享、创建/接收列表、详情、更新、撤销、打开共享、LINK 一次性 token、幂等记录、Outbox 事件、权限模块 SHARE 允许来源接入和统一 API 文档 | `go test ./internal/modules/shares/... ./internal/modules/permissions/... ./internal/app ./internal/platform/httpserver` 通过；应用层测试覆盖 LINK token 摘要、SPACE 目标暂缓、共享不能扩大创建者权限、LINK 打开 token 校验 | 模块 08 尚未整体完成；SPACE 目标共享和 `shared_entries` 命名空间引用、匿名外链、链接密码、真实下载/预览、HTTP 集成和共享缓存失效集成测试待后续 |
| 模块 09：回收与生命周期元数据闭环周期 | 2026-08-10 | OpenAPI 生命周期契约、sqlc 查询、`internal/modules/lifecycle` 领域/应用/Repository/HTTP 适配、进入回收站、回收站分页列表、恢复、发起永久清理、共享 `SOURCE_UNAVAILABLE` 标记、空间安全纪元递增、资料保全清理阻断、Outbox 事件、统一 API 文档和专项调研 | `go test ./...` 通过；应用层测试覆盖进入回收站、根目录删除拒绝、恢复重名冲突、资料保全阻断永久清理和幂等键不同请求冲突 | 已由后续“回收站过期扫描入队周期”补齐自动过期扫描入队；真实对象存储删除、最终 `PURGED` 收敛、资料保全管理接口、归档/冷存储策略、HTTP 集成和大目录性能测试待后续 |
| 模块 09：回收站过期扫描入队周期 | 2026-08-10 | 新增 `scanExpiredRecycleItems` 管理员接口、过期 `ACTIVE` 回收项 PostgreSQL 查询、`LIFECYCLE_PURGE` 后台任务入队编排、资料保全跳过、OpenAPI、sqlc 生成代码、统一 API 文档和开发计划同步 | `go test ./...` 通过；应用层测试覆盖管理员扫描入队、资料保全跳过、普通用户拒绝和非法 `batchSize` | 模块 09 尚未整体完成；`LIFECYCLE_PURGE` 真实处理器、对象存储删除、引用计数、最终 `PURGED` 收敛、HTTP 集成和大目录性能测试待后续 |
| 模块 09：资料保全管理接口周期 | 2026-08-10 | 新增 `placePreservationHold/listPreservationHolds/getPreservationHold/releasePreservationHold` 4 个接口，复用底层 `legal_holds` 表，支持创建幂等、分页筛选、详情查询、解除并发校验、空间安全纪元递增和 Outbox 审计事件 | `go test ./internal/modules/lifecycle/... ./internal/platform/httpserver ./internal/app` 通过；应用层测试覆盖创建资料保全、解除资料保全和普通用户拒绝 | 模块 09 尚未整体完成；资料保全审批流、批量保全、HTTP 集成、大目录性能和真实清理 Worker 接入待后续 |
| 模块 10：PostgreSQL 元数据搜索基础周期 | 2026-08-10 | OpenAPI 搜索契约、sqlc 查询、`internal/modules/search` 领域/应用/Repository/HTTP 适配、名称/扩展名/分类/metadata 筛选、活动目录项候选查询、逐项 `READ_METADATA` 最终权限复核、降级响应、统一 API 文档和专项调研 | `go test ./internal/modules/search/...` 通过；应用层测试覆盖无权候选过滤、至少一个查询条件、metadata 筛选必须成对和匹配字段标记 | 模块 10 尚未整体完成；索引刷新入队已由后续周期补齐，真实 INDEX 处理器、全文/OCR/语义搜索、外部搜索服务、权限安全跨页精确 total、HTTP 集成和大数据量性能测试待后续 |
| 模块 10：索引刷新任务入队周期 | 2026-08-10 | 新增 `enqueueSearchIndexRefreshJobs` 管理员接口，批量校验文档 ID，读取 ACTIVE 文档索引目标，标记 `document_index_states.status=PENDING`，写入 `INDEX` 后台任务，并返回逐项成功/失败结果 | `go test ./...` 通过；应用层测试覆盖系统管理员入队成功、缺失文档单项失败、普通用户拒绝和重复文档参数拒绝 | 模块 10 尚未整体完成；真实 `INDEX` 处理器、全文/OCR/语义搜索、外部搜索服务、权限安全跨页精确 total、HTTP 集成和大数据量性能测试待后续 |

## 13. 下一步

当前基础准备以及模块 01～05 已完成当前边界；模块 06 已完成对象存储适配基线和上传控制面当前周期；模块 07 已完成版本与锁基础接口周期；模块 08 已完成共享基础接口周期；模块 09 已完成回收与生命周期元数据闭环、过期扫描入队和资料保全基础管理周期；模块 10 已完成 PostgreSQL 元数据搜索基础和索引刷新任务入队周期；模块 11 已完成审计基础查询、摘要统计增强和完整性接口当前周期；模块 16 已完成后台任务基础调度、管理员统计、队列延迟摘要、健康摘要、失败原因聚合、过期租约恢复、Outbox/Job 重试、取消、批量死信、批量跳过和批量运维接口周期。Windows 本地阶段已形成收敛说明，后续不再主动扩展边缘控制面；除缺陷修复、文档校对和轻量验证外，主要开发应等待 Linux/SeaweedFS S3 Gateway 环境。由于对象存储集群尚未搭建，模块 06 的真实上传闭环仍不能最终验收，模块 07 的真实版本生成与下载、模块 08 的共享下载/预览、模块 09 的真实对象清理以及模块 10 的全文/OCR/语义搜索也需要回到模块 06/07/12/16 后续周期。

建议执行顺序：

1. 优先搭建 Linux/SeaweedFS S3 Gateway 环境，并按 `docs/File-Workshop-V1.0-Windows本地阶段收敛说明.md` 的启动顺序验证 Bucket、凭据、Multipart、预签名和 Range；
2. 环境可用后回到模块 06，实现 `upload_parts` 分片登记、完成提交、Hash 校验、`storage_objects/document_versions` 写入和下载/Range 契约；
3. 模块 10 搜索的真实 INDEX 处理器、全文内容、模块 12 预览和模块 09 对象清理涉及文件内容、版本或存储对象时必须等待模块 06/07 基线；
4. 若继续停留在 Windows 本地，仅做缺陷修复、文档校对、轻量验证和环境启动清单，不再主动扩展边缘控制面；
5. 模块 16 接入 `USER_CREATED` 个人空间自动初始化前，仍需确认可配置的默认名称和配额策略；
5. 后续每个模块完成时继续追加统一 API 接口文档。

SeaweedFS/S3 继续暂缓，不影响已完成的上传控制面代码编译和单元测试；真实上传、下载和兼容性验收前仍需项目负责人搭建 SeaweedFS S3 Gateway 并确认 Bucket 与凭据。
