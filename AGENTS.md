# File Workshop 项目开发规则

本文件是本仓库所有开发者与编码 Agent 的常驻执行规范，适用于代码、接口、数据库、测试、文档和部署配置。开始任务前必须先阅读本文件，再按需阅读 `docs/` 中与任务相关的章节。

## 1. 规则优先级与文档来源

1. 当前用户明确提出的要求优先于仓库既有文档。
2. 本文件是日常开发的统一入口；未覆盖的业务语义以 `docs/File-Workshop-V1.0-企业级文件管理系统开发设计文档.md` 为准。
3. PostgreSQL 表名、字段名、字段类型、默认值、可空性、主外键、约束、索引、枚举、事务、分区、数据库权限和 Migration 全部以 `docs/File-Workshop-V1.0-数据库设计说明.md` 为唯一权威来源；任何代码、SQL、Migration、sqlc 查询或测试不得自行猜测、缩写、改名或新增数据库字段，主设计文档不再维护重复表结构。
4. 未覆盖的技术选型以 `docs/File-Workshop-V1.0-技术选型说明.md` 为准。
5. 系统模块顺序、模块内工程任务、当前状态和完成记录以 `docs/File-Workshop-V1.0-开发计划.md` 为统一执行台账；开发主线严格映射主设计文档第 6 章的系统模块划分。
6. 文档冲突时：业务语义、安全边界和故障语义以主设计文档为准，数据库结构以数据库设计说明为准，组件与工程工具以技术选型说明为准。
7. 本文件已确认的分页规则覆盖设计文档中旧的 `cursor`、`limit` 分页字段约定。
8. 变更架构形态、数据库、对象存储、权限入口、审计可靠性、文件数据路径或安全边界，必须新增或更新 ADR，不得静默修改。
9. 不确定业务规则时先查文档和现有契约；仍无法确定且会改变外部行为时，停止实现并向用户确认。

## 2. 开发工作方式

- 修改前先检查相关代码、OpenAPI、Migration、测试和文档，不凭文件名猜测行为。
- 只修改完成当前任务所需的范围，保留用户已有改动，不顺手重构无关模块。
- 优先修复根因，不用重复逻辑、硬编码或吞掉错误掩盖问题。
- 新增依赖前说明必要性，优先使用标准库或已冻结依赖；不得仅为少量工具函数引入大型依赖。
- 生成代码必须由固定命令重复生成，禁止手工修改生成产物。
- 每次实现后查看 Diff，并执行与风险相称的格式化、静态检查和测试。
- 不提交密钥、令牌、密码、签名 URL、真实个人信息或生产数据。
- 除非用户明确要求，不直接推送 `main`、发布、部署或执行不可逆数据操作。

### 2.1 开发计划与模块状态同步

- 开始开发任务前，必须先在 `docs/File-Workshop-V1.0-开发计划.md` 中找到对应系统模块和模块内工程任务；没有对应任务时先补充计划再实施。
- 后端开发必须以主设计文档第 6 章的模块 01～16 为主线，工程细节必须归入对应模块，不得用脱离业务模块的技术周期取代开发主线；模块执行顺序可根据依赖、环境、风险和项目优先级灵活调整，并在计划中记录原因和影响。
- 模块和工程任务状态统一使用：`[ ]` 未开始、`[~]` 开发中、`[x]` 已完成、`[!]` 被阻塞、`[-]` 暂缓。
- 模块开始时将模块及当前工程任务改为 `[~]`；遇到真实阻塞时改为 `[!]` 并直接记录原因和解除条件；用户明确暂缓时改为 `[-]`。
- 模块只有在适用的功能、契约、数据库映射、测试、日志、审计和文档全部满足完成定义后，才能改为 `[x]`。部分实现、仅能编译或未完成必要测试不得标记为已完成。
- 每个模块完成时，必须在同一次变更中更新模块状态和“系统模块完成记录”，写明日期、实际交付物、验证证据、未验证项及遗留风险。
- 开始或切换模块时检查依赖和当前模块的完成情况；调整顺序或范围时先同步计划并记录原因与影响，不得因调整顺序而混淆模块边界或遗漏未完成任务。
- 后端开发期间按模块先维护 OpenAPI 契约再实现；每个模块开发完成时，必须在同一次变更中把该模块全部接口追加到统一的 `docs/File-Workshop-V1.0-API接口文档.md`，不得等到全部后端模块完成后再补。后端阶段结束后只进行全量复核和版本冻结。接口测试必须以该文档和对应 OpenAPI 契约为依据，代码、OpenAPI 与接口文档不一致视为缺陷。
- Git 已初始化为 `main` 分支，并绑定 `https://github.com/LURENJIAPD/File-Workshop.git`。用户明确要求“提交”或 `commit` 时，在验证通过并完成本地提交后，默认继续推送到 `origin/main`，除非用户明确要求只做本地提交。历史改写、强制推送、发布、部署和其他高风险 Git 操作仍需用户单独明确授权。

## 3. 底层开发思维

坚决杜绝闭门造车和重复造轮子，同时避免为了调研而调研。开源复用以提高项目整体开发效率为目标，不把每个常规模块的开发变成重复选型流程。

1. 项目级优先选择若干与 File Workshop 定位、技术栈或业务能力相近的成熟开源系统作为长期参考，统一记录其仓库、许可证、维护状态、架构、可参考能力和不适用部分；开发各模块时先查这份项目级参考，不重复从零检索。
2. 启动项目、制定较大方案、变更核心架构，或开发文件存储、权限、审计、搜索、预览、异步任务、WebDAV、AI 等高复杂度或高风险能力前，必须专项检索 GitHub、Gitee、官方仓库和官方文档，并交叉比较多套方案。
3. 用户管理等业务边界清晰、复杂度低、可由现有技术栈可靠实现的常规模块，以及简单 Bug、小改动、纯机械调整和离线任务，不强制单独编制开源调研；可直接参考项目级候选和现有依赖后实现。
4. 调研优先回答“能否直接复用成熟项目、现有依赖或官方实现”，其次才考虑二次开发、局部借鉴或自行实现。成熟方案满足需求时，不得在没有明确收益的情况下重新实现同类基础能力。
5. 决定引入项目、组件或代码时，必须评估许可证（License）、维护状态、最近发布、社区活跃度、安全与供应链风险、架构质量、功能匹配度和适配成本，并冻结版本和锁文件，保留升级与替换路径。
6. 符合需求且许可证兼容的实现优先直接复用；不适合直接引入时，可以提炼可验证的设计思路。复用内容必须遵守版权、NOTICE、署名、源码开放和再分发要求，来源不明或许可证不兼容的内容禁止进入仓库。
7. 引入开源项目不等于无条件接受其实现。权限、安全、审计、文件数据路径、故障语义、数据库权威设计和分页契约仍必须符合本项目规则；必要偏差通过适配层隔离。
8. 只有任务按第 2 条确实需要专项调研且网络不可用时，才将任务标记为阻塞；不得因为常规模块没有单独调研而阻止开发或完成。

## 4. 冻结技术基线

- 架构：模块化单体 + 独立 Go Worker。
- 后端：Go + Gin；底层遵循 `net/http` 标准语义。
- API：OpenAPI 3.1，基础路径 `/api/v1`。
- 数据库：PostgreSQL + pgx + sqlc；Migration 默认使用 Goose。
- 对象存储：MinIO，业务代码只依赖标准 S3 兼容语义。
- 缓存和短期状态：Redis；Redis 不得成为业务事实的唯一来源。
- 异步可靠性：PostgreSQL Outbox + Go Worker；V1.0 不默认引入消息队列。
- 前端：Vue 3 + TypeScript + Vite + Vue Router。
- 前端组件和状态：Element Plus、Pinia、TanStack Vue Query。
- 测试：Go Test、httptest、本地隔离 PostgreSQL/Redis 集成测试、Vitest、Playwright；性能测试默认使用 k6。Testcontainers 仅作为具备 Docker/CI 环境后的可选隔离方案，不是本地开发或项目运行依赖。
- 技术栈变化必须有明确理由、影响分析和 ADR，不得由 Agent 自行替换。

## 5. 仓库与模块组织

目标目录按职责组织：`backend/`、`frontend/`、`deploy/`、`migrations/`、`docs/`、`tests/`、`scripts/`、`tools/`。

- 后端按业务模块组织，禁止把所有 Controller、Service、Repository 在仓库顶层全局平铺。
- 模块内部可划分 `domain`、`application`、`repository`、`transport`、`events`。
- 跨模块调用通过应用服务接口、领域事件或明确的只读查询完成，禁止绕过边界直接访问其他模块的数据表或内部实现。
- 通用能力放入边界清晰的 platform/shared 包，禁止建立无归属的万能 `utils` 包。
- 避免循环依赖；依赖方向应从适配层指向应用层和领域层。

## 6. API 与 OpenAPI 规范

- OpenAPI 是 REST API 的唯一权威契约。接口实现前先更新契约，再生成或校验 Go 类型、TypeScript 类型、客户端和契约测试。
- 普通 API 使用 JSON；JSON 字段和查询参数统一使用 `camelCase`。
- URL 使用复数资源名和稳定资源 ID，不以名称作为唯一定位，不在路径中写产品名。
- 时间对外使用 ISO 8601/RFC 3339，服务端内部统一 UTC。
- 文件二进制不得编码为 Base64；大文件通过对象存储数据面传输。
- 写操作返回稳定错误码；可重试的写操作使用 `Idempotency-Key` 并验证相同键的请求体一致。
- 新增响应字段应保持向后兼容；删除、改名或改变字段语义属于破坏性变更。
- Handler 只负责协议适配、校验、上下文建立、服务调用和响应映射，不承载领域规则。

### 6.1 累积式 API 接口文档

- 全项目只维护一份面向开发、联调和测试的可读接口文档：`docs/File-Workshop-V1.0-API接口文档.md`，内容按系统模块 01～16 分章持续追加。
- OpenAPI 仍是机器可读的唯一权威契约；可读接口文档必须从当前 OpenAPI 和实际实现核对后编制，不得产生字段、状态码或错误码分叉。
- 一个模块只有在其路由、Operation ID、认证方式、请求参数、响应结构、状态码、错误码、Cookie/Header、副作用、示例和测试要点全部写入接口文档，并通过同步校验后，才能标记为 `[x]`。
- 后续接口变更必须在同一次提交中同步 OpenAPI、生成代码、实现、测试和该接口文档；禁止只改其中一层。
- 接口测试按接口文档中的模块章节和测试要点编制；后端全部模块完成后对累积文档执行全量复核、生成冻结版本，不重新另写一份文档。

### 6.2 统一分页规范

所有分页场景，包括 REST 查询参数、JSON 请求体、OpenAPI Schema、Go DTO、TypeScript 类型、前端状态和测试夹具，统一遵守以下规则：

- 分页请求字段只能命名为 `page` 和 `pageSize`。
- 禁止对外使用 `cursor`、`limit`、`offset`、`pageNo`、`pageNum`、`pageIndex`、`current`、`size`、`page_size` 等别名。
- `page` 为从 `1` 开始的整数，默认值为 `1`。
- `pageSize` 为正整数，默认值为 `50`，最大值为 `200`。
- 非整数、`page < 1`、`pageSize < 1` 或 `pageSize > 200` 必须返回统一的参数校验错误，不得静默修正为其他值。
- 所有列表接口必须分页或具有更严格的固定结果上限，禁止无界列表接口和无界 SQL 查询。
- 分页响应至少返回 `items`、`page`、`pageSize`。只有在能够准确且经济地计算时才返回 `total`；不得为了 `total` 对大表执行高成本全量统计。
- `totalPages` 如需返回，必须由 `total` 和 `pageSize` 计算，不得作为独立事实维护。
- 排序必须稳定，最终排序键必须包含唯一资源 ID，避免翻页时重复或遗漏。
- 外部契约固定为 `page`、`pageSize`，但数据库实现仍须避免无索引、无边界的深页扫描；大数据场景应通过索引、稳定排序、查询改写或经评审的内部定位机制优化，不得擅自更换对外字段。
- 前后端、OpenAPI、接口示例和测试必须同时更新；不得仅在某一层做字段转换以长期兼容新的非标准别名。

分页请求示例：

```http
GET /api/v1/files?page=1&pageSize=50
```

分页响应示例：

```json
{
  "items": [],
  "page": 1,
  "pageSize": 50,
  "total": 0
}
```

### 6.3 错误响应

- 错误响应包含稳定的机器可读错误码、用户可读消息、`requestId`，必要时包含结构化 `details`。
- HTTP 状态码表达协议级结果，业务代码表达领域原因；Repository 不返回 HTTP 错误。
- 不把内部堆栈、SQL、文件系统路径、对象存储信息或敏感配置暴露给客户端。
- 前端展示可理解的错误信息，并为可追踪错误显示 Request ID。

## 7. 后端 Go 规范

- Go 代码必须通过 `gofmt`；遵循标准 Go 命名和错误处理习惯。
- `gin.Context` 只能存在于 HTTP 适配层，不得传入应用服务、领域服务、Repository、存储适配器、Worker 或 Goroutine。
- 应用层统一使用 `context.Context`，外部调用和数据库查询必须有可取消的超时。
- Handler 禁止直接执行 SQL、访问对象存储、拼装权限规则、修改审计记录或控制跨模块事务。
- 事务由应用服务控制；事务中禁止执行长时间对象存储、杀毒、预览、网络模型或 AI 调用。
- API DTO、应用命令/结果和领域对象分离，禁止把数据库模型直接作为 API 响应。
- 使用显式构造函数注入；禁止全局可变业务状态。
- 时间、随机数和 ID 生成器应可注入，以支持确定性测试。
- 重试仅用于可证明幂等的操作，并使用有上限的退避策略。
- 每个 Goroutine 必须有生命周期、取消、错误收敛和退出机制，不得泄漏后台任务。
- 错误应保留原因链并在边界统一映射；禁止无说明地吞掉错误或只记录后继续成功返回。

## 8. 数据库与 Migration 规范

- 开发任何数据库相关功能前，必须先查阅 `docs/File-Workshop-V1.0-数据库设计说明.md` 中对应表定义，并逐项核对表名、字段名、类型、默认值、可空性、主外键、约束和索引；禁止凭业务描述或常见命名习惯推测数据库结构。
- 数据库设计说明中不存在的表、字段、枚举、索引或关系一律视为“尚未设计”，不准在代码、SQL、Migration、sqlc、测试夹具或临时兼容逻辑中擅自补造。确有需要时必须停止实现，先提出设计变更，按用户确认结果更新数据库设计说明和 Migration，再继续编码。
- 数据库设计说明、已导入 Schema、SQL 脚本或代码之间不一致时，不得选择一个看似合理的名称继续开发；必须报告差异，并以数据库设计说明为基准完成受控校正。
- API JSON 字段仍遵守 `camelCase`，但必须明确映射到数据库设计中真实存在的字段；API DTO、领域对象或开源项目中的字段名不能反向证明数据库中存在同名字段。
- 数据库实体表的主键字段必须使用语义化 `snake_case` 名称，统一按实体命名为 `<entity>_id`，例如 `users.user_id`、`documents.document_id`；禁止使用缺少实体语义的通用 `id`。这里的点号仅表示“表名.字段名”，不属于实际字段名。
- 实体 ID 使用 PostgreSQL 原生 `uuid` 并由 Go 侧生成 UUIDv7；表示具体时刻的数据库字段统一使用 `TIMESTAMPTZ`，禁止恢复 `UUID/ULID` 待选状态或使用裸 `TIMESTAMP` 表示时刻。
- 外键字段按所表达的关系语义命名，例如 `user_id`、`owner_user_id`、`created_by_user_id`；存在多个指向同一张表的外键时必须能够从字段名区分各自角色。
- 纯关联表可以使用具备明确语义的联合主键，配置等特殊表可以使用稳定自然键；不得为了形式统一而额外增加无业务作用的代理主键。
- SQL 必须参数化，禁止拼接未受信任输入。
- 查询必须可取消并设置超时；列表查询必须分页或有明确上限。
- 高风险和高频查询需要索引依据，并使用执行计划验证。
- 避免 N+1 查询、无条件全表扫描和不受控的深分页 OFFSET。
- 事务范围保持最小；业务事实和对应 Outbox 事件在同一 PostgreSQL 事务写入。
- PostgreSQL 保存业务事实，对象存储保存二进制；搜索索引、缓存和向量索引必须可从事实源重建。
- 已发布 Migration 禁止修改，只能追加新 Migration。
- Migration 必须支持空库创建和升级路径测试；大表变更采用扩展、迁移、收敛的分阶段方案。
- 外键删除策略必须显式；JSONB 字段必须有明确 Schema 和版本策略。
- 审计记录禁止通过普通业务接口更新或删除。

## 9. 文件、对象存储与异步任务

- 应用代码通过存储适配器使用 S3 兼容接口，不依赖 MinIO 私有 URL、Bucket 结构或管理 API。
- 对象 Key 由系统生成，禁止直接使用用户文件名或可预测路径。
- 大文件使用分片直传和流式读写，禁止在 API 进程中一次性读入完整文件。
- 浏览器和客户端不得获得对象存储长期凭据；预签名 URL 使用短有效期和最小权限。
- 上传完成后由异步任务执行 Hash、病毒扫描、预览、文本提取和索引。
- Worker 必须支持幂等、并发安全、有限重试、退避、失败记录和死信状态。
- 不可信文件的解压、转换、预览和扫描必须隔离并限制 CPU、内存、磁盘、网络和执行时间。

## 10. 权限、安全与审计

- 所有入口，包括 Web、REST API、WebDAV、搜索、分享、AI 和后台任务，必须使用统一身份、权限和审计语义。
- 前端隐藏按钮只用于改善体验，服务端必须对具体资源和动作再次授权。
- 搜索、缓存和索引只提供候选结果，最终返回前必须执行服务端权限校验；不确定时少返回，禁止越权返回。
- 资源级授权不得只依赖网关或粗粒度中间件。
- 密码使用安全哈希；Session、Token、对象存储密钥和签名信息不得进入日志。
- 对登录、权限、共享、删除、恢复、下载、管理操作和 Agent 写操作记录必要审计。
- 安全相关失败不得降级为放行；Redis、搜索或 AI 故障不得造成业务事实丢失或越权。
- 任何外部输入、文件、压缩包、Webhook 和 AI 输出都视为不可信，必须进行 Schema、大小、类型和权限校验。

## 11. 前端规范

- 使用 Vue 3 Composition API 和 TypeScript strict；不得使用 `any` 绕过可表达的类型问题。
- API 类型和客户端从 OpenAPI 生成，禁止手工重复维护请求/响应 DTO。
- Pinia 保存客户端状态；TanStack Vue Query 管理服务端数据、缓存、重试和失效，禁止把服务端事实长期复制到多个 Store。
- 统一使用选定设计方案的设计令牌和组件语言，不在项目中混用多个大型组件库。
- 表单必须有明确标签、校验、错误反馈和提交状态；交互组件支持键盘操作和可见焦点。
- 大列表使用虚拟化，树结构按需加载，搜索和筛选防抖，预览按需加载。
- 上传队列支持暂停、恢复、取消、失败重试和刷新恢复；大文件 Hash 放入 Web Worker。
- 不在浏览器持久化敏感 Token、长期对象存储凭据或不必要的文件内容。
- 页面必须处理加载、空数据、错误、无权限和部分降级状态，并适配项目约定的桌面与移动断点。
- 所有分页组件和请求状态只能使用 `page`、`pageSize`，切换筛选条件或排序时将 `page` 重置为 `1`。

## 12. 测试规范

- 新功能和缺陷修复必须有能够失败于修改前、通过于修改后的测试。
- 后端单元测试覆盖领域规则和纯函数；集成测试覆盖 PostgreSQL、Redis、对象存储及事务边界。
- API 变更必须更新 OpenAPI 校验和契约测试。
- 前端使用 Vitest 测试组件、工具和状态逻辑；使用 Playwright 覆盖登录、上传、权限、分享、搜索和回收站等关键流程。
- 权限测试必须同时覆盖允许、拒绝、越权、权限撤销和缓存失效。
- 并发敏感功能测试重复提交、冲突、锁、重试和幂等性。
- 分页测试至少覆盖默认值、第一页、最后一页、空结果、非法值、最大 `pageSize`、超限 `pageSize`、稳定排序和筛选后重置页码。
- 不依赖真实生产服务或不可重复的外部环境；测试数据必须隔离且可清理。
- 不得通过删除、跳过或放宽有效断言来让测试通过。

## 13. 日志、指标与可观测性

- 使用结构化日志，至少按场景记录时间、级别、Request ID、Trace ID、主体、模块、操作、资源 ID、错误码和耗时。
- 禁止记录密码、会话密钥、完整 Token、签名 URL、对象存储密钥和不必要的文件内容。
- 关键调用应传播 Request ID 和 Trace 上下文。
- 新增关键业务流程时同步考虑成功率、延迟、失败原因、队列积压和安全事件指标。
- 日志用于诊断，稳定错误码用于程序判断，指标用于趋势和告警，三者不得互相替代。

## 14. 代码质量与提交规范

- 提交前运行仓库已有的格式化、静态检查、单元测试、集成测试和构建命令；若环境缺失，应明确记录未运行项和原因。
- Go 至少执行 `gofmt`、`go vet ./...`、`go test ./...`；前端命令以锁文件和 `package.json` 为准。
- 不提交调试输出、临时文件、构建制品或编辑器私有配置。
- 提交前缀使用 `feat:`、`fix:`、`refactor:`、`test:`、`docs:`、`perf:`、`security:`、`build:` 或 `chore:`。
- Git 提交标题和正文统一使用中文；上述英文前缀保留，前缀后的内容必须使用中文。
- 提交说明应解释变更原因和影响，不只罗列文件。
- 兼容性、性能和安全结论必须有测试、测量或文档依据，不得凭感觉声明。

## 15. 完成定义

一项功能只有在适用项全部完成后才算完成：

- 代码实现和必要重构；
- OpenAPI 契约及生成代码；
- 数据库 Migration；
- 单元、集成、契约、权限和端到端测试；
- 错误处理、结构化日志、指标和审计；
- 安全检查与依赖审查；
- 涉及项目级选型、高复杂度能力或实际引入第三方代码时，已完成相应开源方案与许可证评估；常规模块不强制单独调研；
- 用户文档、开发文档或 ADR；
- 当前模块接口已追加到统一 API 接口文档，并与 OpenAPI 和实现一致；
- 格式化、静态检查、测试和构建通过；
- 明确兼容、回滚或前滚方案；
- 最终 Diff 不包含无关修改或敏感信息；
- 对应系统模块和工程任务已在开发计划中标记为完成，并填写系统模块完成记录。

若受本地环境限制无法执行某项验证，交付说明必须列出未验证内容、原因、风险和建议执行命令，不得把“未运行”描述为“已通过”。
